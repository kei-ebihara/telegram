<div>
  <% if current_user.likes.find_by(post_id: post.id) %>
    <%= link_to "♥", unlike_path(post), method: :delete, class: "like" %>
  <% else %>
    <%= link_to "♡", like_path(post), method: :post, class: "unlike" %>
  <% end %>
  <%= post.likes_count %>
</div>